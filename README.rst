A sample extension for Ava
==============================
This serves as the skeleton for an extension package project.

An extension is a regular Python package in egg format with an entry point
named 'ava.extension'.


How to Run
***************

.. code-block:: bash

    python bootstrap.py
    bin/buildout
    bin/ava